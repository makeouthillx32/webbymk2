import type { Metadata } from "next";
import CamerasPage from "@/zones/tank/public/CamerasPage";

export const metadata: Metadata = {
  title: "Cameras | Tank",
  description:
    "Live camera feeds discovered automatically from the Tank receiver.",
};
export default CamerasPage;
