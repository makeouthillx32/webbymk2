// zones/shop/src/app/collections/[slug]/page.tsx
// Re-export wrapper — all logic lives in the core module.
export { default, generateMetadata, generateStaticParams } from "@/zones/shop/collections/[slug]/Page";
