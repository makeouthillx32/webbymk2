"use client";

export default function HeroHead() {
  return (
    <div className="absolute inset-0 pointer-events-none z-10 h-[10vh] md:h-[15vh]">
      {/* This is just the space for the header */}
    </div>
  );
}