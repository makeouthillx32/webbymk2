import type { ProductRow } from "../types";

// Placeholder tab — intentionally blank for now, per request. Fill in later.
interface LabsTabProps {
  detail: ProductRow;
}

export function LabsTab({ detail }: LabsTabProps) {
  return <div />;
}
