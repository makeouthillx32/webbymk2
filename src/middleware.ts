// The active middleware for this src/app project lives here.
// Keep the implementation in the project-root middleware.ts so there is only
// one multi-zone/Supabase middleware to maintain.
export { config, middleware } from "../middleware";
