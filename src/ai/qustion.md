<!--
  REDACTED — this file previously contained a raw TUI screenshot dump that
  exposed real infrastructure values (DDNS hostname, both LAN node IPs,
  internal service/port inventory). Since this repo is public on GitHub,
  that's a topology leak — removed 2026-06-08.

  If you need a sample/reference render of the infra panel for docs or AI
  context, regenerate it with placeholder values (e.g. 192.168.x.x, role
  labels like NODE-A/NODE-B instead of real hostnames) rather than a live
  capture.
-->
