
**obsidian-architect** - created vault/Architecture/ink-engine-architecture.md from src/ink/ scan

- read ENGINE_STATUS.md and APP_MIGRATION.md for context
- read runtimeInk.ts (adapter), root.ts (entry), reconciler.ts (React integration), renderer.ts (DOM→screen), screen.ts (CharCell grid), dom.ts (virtual DOM)
- documented complete architecture: React → Reconciler → DOM → Yoga layout → Renderer → Output → Screen buffer → Diff → Terminal
- captured all 6 layers: Entry/Lifecycle, React Integration, DOM Tree, Yoga Layout, Rendering, Diff/Output
- added critical invariants (Yoga not constrained by terminal height in alt-screen, screen buffer clips, hasRenderedFullFrame timing, event handler routing, CharPool persistence, focus ownership, Yoga cleanup)
- documented 6 known footguns: flex-shrink chain, blit truncation, hasRenderedFullFrame reset timing, text measure cache, measure function timing, alt-screen mouse/rawmode pairing
- added render loop sequence, component primitives (Box, Text, Newline, Spacer), detached preview mode, migration notes
- updated vault/Home.md to reference the new arch doc in both "Start here" and "Recent (2026-06-02)"


**obsidian-decide + obsidian-emerge** - extracted architectural decisions and synthesized patterns
- read [[TUI/fix-yoga-height-chain]], [[TUI/ink-engine-cutover-decision]], [[TUI/session-2026-06-01-local-ink-engine-complete]], and 7 other TUI notes
- identified 5 key decisions: (1) cut over to local ink engine, (2) screen buffer clips not yoga, (3) useTermHeight for windowing not yoga props, (4) first-frame scheduling deferred until layout complete, (5) React instance unification via reactRuntime.js
- captured full reasoning, what was rejected, and consequences for each
- identified 5 emerging patterns: (1) silent failures amplified by damage-based diff, (2) hook values mixing with declarative layout props, (3) first-frame scheduling breaks when layout incomplete, (4) cutover reveals masked bugs, (5) configuration persistence beats state memory
- created [[Architecture/key-decisions]] reference doc (read before making any changes to rendering/layout)
- created [[Brain/patterns-2026-06-02]] synthesis doc (visible across all TUI notes, not obvious from any single one)
- updated [[Home.md]] with links to both new docs


**cli-env-commands-doc** - documented UNAXIS CLI env commands and IPC bridge
- read src/ink/hooks/useIpcBridge.ts lines 555–1054 for env command handlers
- read src/ink/agent-client.ts lines 1–920 for Docker API wrapper functions
- read vault/TUI/env-security-commands.md (existing security audit context)
- read vault/Environments/env-panel-architecture.md (existing panel architecture)
- created vault/Environments/cli-env-commands.md comprehensive reference:
  - Quick reference table: 12 env subcommands
  - Detailed section per command: syntax, usage, agent calls, return formats, exit codes
  - IPC routing diagram (CLI → IPC bridge → agent-client → agent HTTP → Docker API)
  - Environment targeting logic (isDefaultTarget vs explicit <name>)
  - ECDSA P-256 signature implementation (TOFU pairing model)
  - Output format examples (table, JSON, error messages)
  - Design notes: why separate commands, security audit vs audit-image split, layer inspection rationale
- appended cross-link from vault/Environments/env-panel-architecture.md → cli-env-commands
- appended links to vault/Home.md Start here section



**synthesis-env-management** - final documentation sweep combining env architecture + CLI + security
- read vault/Environments/env-panel-architecture.md (TUI panel, data flow, agent API, Supabase schema)
- read vault/Environments/cli-env-commands.md (12 CLI subcommands, IPC routing, ECDSA signatures)
- read vault/TUI/env-security-commands.md (security audit, image scanning, event log)
- created vault/Brain/synthesis-env-management.md single coherent model:
  - Full Picture diagram (TUI panel → ECDSA signed HTTP → agent binary → Docker API)
  - POWER vs L0V3 comparison (bind-mount code vs container swap, local vs remote)
  - What "full control" means (dashboard, lifecycle, logs, security audit, health checks, agent management)
  - How two environments actually connect (Supabase registration, agent-client HTTP, IPC bridge, agent binary)
  - TUI panel details (keyboard commands, nested views)
  - CLI commands table (12 subcommands across status/health/lifecycle/logs/security/audit/update)
  - Security commands (env security, env audit-image, env events — added 2026-05-28)
  - Agent update pipeline (two-phase for L0V3 with rollback safety)
  - Design decisions (all envs simultaneously live, TOFU pairing, Portainer API, rollback safety, inline error detail)
  - Key source files map
  - Summary for future Claude (how to think about env management)
- appended to vault/Home.md (Start here section)
- appended to 2026-06-02.md daily log
